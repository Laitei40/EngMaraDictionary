name = "adminmaradic"
main = "worker.js"
compatibility_date = "2026-02-19"

[assets]
directory = "./public"
binding = "ASSETS"

# Service Binding to the API worker (Worker-to-Worker, no public network hop)
[[services]]
binding = "API"
service = "engmaradictionary"
